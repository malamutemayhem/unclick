{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "render",
  "description": "Hidden render windows: remote pages may emit only the DOM-snapshot event back to the app. No other IPC is granted to remote content.",
  "windows": ["render-*"],
  "remote": {
    "urls": ["https://*", "http://*"]
  },
  "permissions": ["core:event:allow-emit"]
}
