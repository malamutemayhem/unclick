name: TestPass PR Check

on:
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      pack_id:
        description: Slug of the TestPass pack to run.
        required: false
        default: testpass-core
      profile:
        description: Run profile (smoke, standard, deep).
        required: false
        default: smoke
      server_url:
        description: Optional target MCP server URL.
        required: false
        default: https://unclick.world/api/mcp

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  testpass:
    name: TestPass smoke run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Check TestPass token availability
        id: token
        shell: bash
        env:
          TESTPASS_TOKEN: ${{ secrets.TESTPASS_TOKEN }}
        run: |
          if [ -n "${TESTPASS_TOKEN:-}" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve TestPass target
        id: testpass-target
        if: ${{ steps.token.outputs.has_token == 'true' }}
        uses: actions/github-script@v9
        with:
          script: |
            const productionBase = 'https://unclick.world';
            let previewBase = '';

            if (context.eventName === 'pull_request') {
              const comments = await github.paginate(github.rest.issues.listComments, {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                per_page: 100,
              });
              for (const comment of comments.reverse()) {
                if (comment.user?.login !== 'vercel') continue;
                const match = comment.body?.match(/\[Preview\]\((https:\/\/[^)\s]+)\)/);
                if (match?.[1]) {
                  previewBase = match[1].replace(/\/+$/, '');
                  break;
                }
              }
            }

            const base = previewBase || productionBase;
            core.setOutput('api_url', `${base}/api/testpass-run`);
            core.setOutput('server_url', `${base}/api/mcp`);
            core.setOutput('source_base', base);
            core.info(`TestPass target base: ${base}`);

      - name: Wait for Vercel preview
        if: ${{ steps.testpass-target.outputs.source_base != '' && steps.testpass-target.outputs.source_base != 'https://unclick.world' }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          for i in {1..24}; do
            state="$(gh api "repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/status" --jq '.statuses[]? | select(.context == "Vercel") | .state' 2>/dev/null | head -n 1 || true)"
            if [ "$state" = "success" ]; then
              echo "Vercel preview is ready."
              exit 0
            fi
            echo "Waiting for Vercel preview (${state:-unknown})..."
            sleep 10
          done
          echo "Vercel preview was not confirmed ready; continuing so TestPass can report the real response."

      - name: Run TestPass
        id: testpass
        if: ${{ steps.token.outputs.has_token == 'true' }}
        # Don't abort the workflow on infra failures - we still want to post a
        # PR comment that explains what happened so reviewers can tell infra
        # noise (stale token, transport blip) from a real product regression.
        continue-on-error: true
        uses: ./.github/actions/testpass
        with:
          token: ${{ secrets.TESTPASS_TOKEN }}
          pack_id: ${{ inputs.pack_id || 'testpass-core' }}
          profile: ${{ inputs.profile || 'smoke' }}
          server_url: ${{ inputs.server_url || steps.testpass-target.outputs.server_url || 'https://unclick.world/api/mcp' }}
          api_url: ${{ steps.testpass-target.outputs.api_url || 'https://unclick.world/api/testpass-run' }}

      - name: Post PR comment
        # Run regardless of TestPass step outcome - infra failures still need a
        # human-readable comment so the PR doesn't look mysteriously broken.
        if: ${{ always() && github.event_name == 'pull_request' && steps.token.outputs.has_token == 'true' }}
        uses: actions/github-script@v9
        env:
          RUN_ID: ${{ steps.testpass.outputs.run_id }}
          STATUS: ${{ steps.testpass.outputs.status }}
          PASS_RATE: ${{ steps.testpass.outputs.pass_rate }}
          FAIL_COUNT: ${{ steps.testpass.outputs.fail_count }}
          TOTAL: ${{ steps.testpass.outputs.total }}
          RESPONSE: ${{ steps.testpass.outputs.response }}
          FAILURE_KIND: ${{ steps.testpass.outputs.failure_kind }}
          HTTP_CODE: ${{ steps.testpass.outputs.http_code }}
        with:
          script: |
            const runId = process.env.RUN_ID || '(unknown)';
            const status = process.env.STATUS || 'unknown';
            const passRateRaw = parseFloat(process.env.PASS_RATE || '0');
            const passRate = Number.isFinite(passRateRaw) ? passRateRaw : 0;
            const failCount = parseInt(process.env.FAIL_COUNT || '0', 10) || 0;
            const total = parseInt(process.env.TOTAL || '0', 10) || 0;
            const failureKind = process.env.FAILURE_KIND || 'unknown';
            const httpCode = process.env.HTTP_CODE || '000';

            let responseJson = {};
            try { responseJson = JSON.parse(process.env.RESPONSE || '{}'); }
            catch { responseJson = {}; }
            const summary = responseJson.verdict_summary || {};
            const failureDetails = Array.isArray(responseJson.failure_details)
              ? responseJson.failure_details
              : [];
            const authReason =
              typeof responseJson.auth_reason === 'string' ? responseJson.auth_reason : '';
            const howToFix =
              typeof responseJson.how_to_fix === 'string' ? responseJson.how_to_fix : '';

            const isInfra = failureKind && failureKind.startsWith('infra_');

            // Infra path: short, friendly, "retry the workflow" tone. Don't
            // dump a verdict table - the numbers are all zero and would just
            // look like a real failure.
            if (isInfra) {
              const headlines = {
                infra_auth:      ':warning: TestPass: infra issue (auth)',
                infra_http:      ':warning: TestPass: infra issue (API error)',
                infra_transport: ':warning: TestPass: infra issue (transport)',
              };
              const authReasonSuffix = authReason ? ` (${authReason})` : '';
              const explanations = {
                infra_auth:
                  `The TestPass API rejected the provided bearer token${authReasonSuffix} ` +
                  `(HTTP ${httpCode}). ` +
                  (howToFix ||
                    `Use a valid Supabase JWT or uc_ UnClick API key in ` +
                    `\`TESTPASS_TOKEN\`, then re-run the workflow.`),
                infra_http:
                  `The TestPass API returned HTTP ${httpCode}. This is a ` +
                  `service-side issue, **not** a problem with this PR. ` +
                  `Re-run the workflow in a few minutes.`,
                infra_transport:
                  `Could not reach the TestPass API (DNS, connection, or ` +
                  `timeout). This is **not** a problem with this PR. ` +
                  `Re-run the workflow.`,
              };

              const body = [
                `### ${headlines[failureKind] || ':warning: TestPass: infra issue'}`,
                ``,
                explanations[failureKind] || `Infra failure: \`${failureKind}\` (HTTP ${httpCode}).`,
                ``,
                `_This comment is posted automatically because TestPass failed before it could check anything. The PR itself has not been evaluated._`,
              ].join('\n');

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
              return;
            }

            // Product path: real verdicts, real table.
            const statusEmoji =
              status === 'complete' && failCount === 0 ? ':white_check_mark: PASS' :
              status === 'failed' || failCount > 0    ? ':x: FAIL' :
              status === 'running'                     ? ':hourglass_flowing_sand: RUNNING' :
              ':grey_question: UNKNOWN';

            const cell = (label, n) => `| ${label} | ${n ?? 0} |`;
            const pct = (passRate * 100).toFixed(1);
            const mdCell = (value) => String(value ?? '').replace(/\|/g, '\\|').replace(/\r?\n/g, ' ');
            const shownFailures = failureDetails.slice(0, 8);
            const failureLines = failureDetails.slice(0, 8).flatMap((item) => [
              `| \`${mdCell(item.check_id || 'unknown')}\` | ${mdCell(item.severity)} | ${mdCell(item.title)} |`,
              item.on_fail_comment ? `|  |  | _${mdCell(item.on_fail_comment)}_ |` : null,
            ]).filter(Boolean);
            const missingFailureDetails = failCount > 0 && failureLines.length === 0;

            const body = [
              `### TestPass: ${statusEmoji}`,
              ``,
              `**Run:** \`${runId}\``,
              `**Status:** \`${status}\``,
              `**Pass rate:** ${pct}%`,
              ``,
              `| Verdict | Count |`,
              `| --- | ---: |`,
              cell(':white_check_mark: check', summary.check),
              cell(':x: fail', summary.fail),
              cell(':grey_question: other', summary.other),
              cell(':heavy_minus_sign: n/a', summary.na),
              cell(':hourglass: pending', summary.pending),
              cell('**total**', total),
              ...(missingFailureDetails ? [
                ``,
                `**Why this is thin:** the TestPass API response did not include per-check failure details. That usually means the live API has not deployed the diagnostic payload yet, so use the run id above for database lookup or re-run after deployment.`,
              ] : []),
              ...(failureLines.length > 0 ? [
                ``,
                `**Failed checks shown:** ${shownFailures.length}`,
                ``,
                `| Check | Severity | What failed |`,
                `| --- | --- | --- |`,
                ...failureLines,
              ] : []),
            ].join('\n');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body,
            });

      - name: Explain Dependabot TestPass skip
        if: ${{ always() && github.event_name == 'pull_request' && github.actor == 'dependabot[bot]' && steps.token.outputs.has_token != 'true' }}
        uses: actions/github-script@v9
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: [
                '### TestPass: skipped for Dependabot',
                '',
                'GitHub does not expose repository secrets to this Dependabot PR, so `TESTPASS_TOKEN` is unavailable and TestPass cannot run from this event.',
                '',
                '_This is an auth/permissions skip, not a product test failure. Maintainer-triggered checks still run fail-closed when a token is available._',
              ].join('\n'),
            });

      - name: Fail if TestPass token is missing for trusted events
        if: ${{ always() && steps.token.outputs.has_token != 'true' && (github.event_name != 'pull_request' || github.actor != 'dependabot[bot]') }}
        shell: bash
        run: |
          echo "TESTPASS_TOKEN is missing. TestPass can only be skipped for Dependabot PRs where GitHub withholds secrets." >&2
          exit 1

      - name: Fail job unless TestPass completed cleanly
        # Keep PR trust fail-closed: only a complete TestPass run with zero
        # failed checks is green. Running, unknown, and infra states must stay
        # visible/retryable instead of looking like evaluated product passes.
        if: ${{ always() && steps.token.outputs.has_token == 'true' && (steps.testpass.outputs.status != 'complete' || steps.testpass.outputs.fail_count != '0') }}
        shell: bash
        env:
          STATUS: ${{ steps.testpass.outputs.status }}
          FAIL_COUNT: ${{ steps.testpass.outputs.fail_count }}
          FAILURE_KIND: ${{ steps.testpass.outputs.failure_kind }}
          HTTP_CODE: ${{ steps.testpass.outputs.http_code }}
        run: |
          echo "TestPass did not complete cleanly." >&2
          echo "status=${STATUS:-unknown}" >&2
          echo "fail_count=${FAIL_COUNT:-unknown}" >&2
          echo "failure_kind=${FAILURE_KIND:-unknown}" >&2
          echo "http_code=${HTTP_CODE:-000}" >&2
          exit 1
